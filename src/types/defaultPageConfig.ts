import { type ScrollDirection } from "@/visualizer/types/scrollDirection";
import { type Theme } from "@/visualizer/types/theme";

export interface DefaultPageConfig {
  theme: Theme;
  scrollDirection: ScrollDirection;
}
